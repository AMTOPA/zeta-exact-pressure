name: joint-pressure-discovery-manual

on:
  workflow_dispatch:

permissions:
  contents: read
  actions: read

jobs:
  certify:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Automatic discovery certification paused while the active candidate is replaced."
