name: archived-second-exchange-17term-certificate

on:
  workflow_dispatch:

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "The second-exchange 17-term certificate is already promoted to candidate.json."
          echo "Certified evidence: workflow 31610179703, artifact 9147378469."
          echo "Use the dedicated multilag workflow for the active auxiliary-certificate experiment."
