name: multilag-auxiliary-certificate-paused

on:
  workflow_dispatch:

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Automatic trigger paused while the active auxiliary certificate is switched from the exploratory odd/even tilt to the stronger uniform pressure supporting line."
          echo "The already-running table-generation workflow remains pinned to its original commit and is unaffected."
