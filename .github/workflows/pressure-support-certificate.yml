name: optimized-pressure-supporting-line-certificate-paused

on:
  workflow_dispatch:

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Automatic trigger paused while the auxiliary target is lowered from the floating optimum toward the exact global no-loss threshold."
          echo "Existing runs remain pinned to their original commits."
