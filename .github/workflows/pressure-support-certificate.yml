name: pressure-supporting-line-certificate-paused

on:
  workflow_dispatch:

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Automatic trigger paused while the auxiliary 6/5-pressure distribution is upgraded by adversarial exchange."
          echo "Existing runs remain pinned to their original commits."
